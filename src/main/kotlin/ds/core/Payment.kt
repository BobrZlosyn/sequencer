package ds.core

class Payment (type: PaymentType, money: Int, sequence: Int, id: Int, name: String) {
    var type = type;
    var money = money;
    var sequence = sequence
    var id = id
    var name = name

}